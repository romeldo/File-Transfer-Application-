#include <netdb.h>
#include <sys/types.h>
#include <sys/socket.h>
#include <netinet/in.h>
#include <stdlib.h>
#include <strings.h>

#define BUFLEN 256

int main ()
{
char str[50],str1[50],buffer[256];
int i=0; 
	printf("Filename to open: \n");
	scanf("%s",&str);
	FILE *fp = fopen (str, "r");
	for ()




return 0;
}
